# 记账 App · 开发工作流

> 目标：自己用的最简记账 PWA，安卓手机端
> 技术栈：Vite + 原生 JS + localStorage + 纯 CSS 图表（零框架、零图表库）
> 制定日期：2026-09-16

---

## 🎯 技术选型说明

| 项 | 选择 | 理由 |
|----|------|------|
| 框架 | **无框架，原生 JS** | 功能简单，引框架反而增加心智负担 |
| 构建 | **Vite** | 给热重载 + 打包，产物仍是纯静态文件 |
| 存储 | **localStorage** | 同步 API 最简单；5MB 约能存几万条，个人记账绰绰有余 |
| 图表 | **纯 CSS 条形图** | 分类占比用横向条就够了，不引任何图表库 |
| 部署 | **免费静态托管** | PWA 的 Service Worker 要求 HTTPS |

**为什么不用 IndexedDB**：localStorage 够用且代码量少一半。将来数据量真的大了再迁移，`store.js` 是唯一要改的地方。

---

## 📐 数据模型

```js
// 一条记录
{
  id: "1737000000000",     // 时间戳作 id
  amount: 32.5,            // 金额（正数，方向由 type 决定）
  type: "expense",         // "expense" 支出 | "income" 收入
  category: "餐饮",         // 分类
  note: "午饭",            // 备注，可空
  date: "2026-09-16",      // 日期 YYYY-MM-DD
  createdAt: 1737000000000 // 创建时间
}
```

**存储键名**：`ledger.records.v1`
（带版本号，将来改结构时方便迁移）

**预置分类**（先写死，不做自定义）：
- 支出：餐饮、交通、购物、居住、娱乐、医疗、其他
- 收入：工资、兼职、红包、其他

---

## 🪜 阶段推进

每个阶段都有**验收标准** —— 达不到就不往下走，避免问题堆积。

---

### 阶段 0 · 骨架跑通
**目标**：手机上能看到一个页面

- [ ] 创建项目目录 `G:\鲸鱼娘工作目录\记账app\`
- [ ] `npm create vite@latest . -- --template vanilla`
- [ ] `npm install`
- [ ] `npm run dev -- --host`（`--host` 是关键，否则手机连不上）
- [ ] 手机浏览器访问 `http://<电脑IP>:5173`

**验收**：手机屏幕显示 Vite 默认页面
**踩坑预警**：连不上通常是 Windows 防火墙拦了，需要放行 Node

---

### 阶段 1 · 数据层
**目标**：先把读写封装好，后面所有界面都调它

- [ ] 写 `src/store.js`，导出四个函数：
  - `addRecord(record)` — 新增
  - `deleteRecord(id)` — 删除
  - `getRecords()` — 取全部（按日期倒序）
  - `getStats(month)` — 传入 `"2026-09"`，返回合计与分类占比

- [ ] 首屏加载时写入两条假数据，方便调试

**验收**：浏览器控制台执行 `store.getStats("2026-09")` 返回正确数字
**为什么先做这个**：数据层是地基。地基对了，界面只是换个方式调它

---

### 阶段 2 · 记一笔（第一个能用的功能）
**目标**：能真的记下一条

- [ ] 顶部大号金额输入（`inputmode="decimal"` 唤起数字键盘）
- [ ] 支出/收入切换按钮
- [ ] 分类九宫格选择
- [ ] 备注输入框（可选）
- [ ] 日期默认今天，可改
- [ ] 保存按钮 → 调 `store.addRecord()` → 清空表单

**验收**：手机上记一笔，刷新页面，数据还在

---

### 阶段 3 · 记录列表
**目标**：能看到记过什么

- [ ] 按天分组显示（日期作小标题，当天合计显示在标题右侧）
- [ ] 每条显示：分类、备注、金额（支出红/收入绿）
- [ ] 删除：长按或左滑，弹确认

**验收**：记账和列表能互相印证，删掉的确实消失

---

### 阶段 4 · 统计
**目标**：知道钱花哪了

- [ ] 月份切换（← 2026-09 →）
- [ ] 顶部两块：本月支出合计、本月收入合计
- [ ] 分类占比：横向 CSS 条形图，按金额降序
  - 每行：分类名 + 条形 + 金额 + 百分比

**验收**：随手记 5 笔，手算合计和占比，与页面一致

---

### 阶段 5 · PWA 化
**目标**：像 app 一样用

- [ ] `public/manifest.json`：名称、图标、主题色、`display: "standalone"`
- [ ] 图标：512×512 和 192×192 各一张（`public/icons/`）
- [ ] `public/sw.js`：Service Worker 缓存静态资源
- [ ] `index.html` 里注册 Service Worker
- [ ] 加 `<meta name="theme-color">` 和 viewport（含 `viewport-fit=cover` 适配刘海屏）

**验收**：手机 Chrome 出现「添加到主屏幕」，加到桌面后图标正常，**断网也能打开**

**⚠️ 关键**：Service Worker 只在 **HTTPS 或 localhost** 下生效。
局域网 IP 访问时 PWA 功能不会启用 —— 所以这一步的完整验收要等阶段 6 部署完。

---

### 阶段 6 · 部署
**目标**：随时随地能用

推荐顺序（都是免费 + 自带 HTTPS）：

| 方案 | 特点 |
|------|------|
| **Vercel** | 最省事，连 GitHub 仓库后自动部署 |
| **Netlify** | 同上，可拖拽文件夹直接部署 |
| **GitHub Pages** | 免费稳定，但要配 GitHub Actions |

- [ ] `npm run build` 产出 `dist/`
- [ ] 部署 `dist/`
- [ ] 手机访问线上地址 → 添加到主屏幕

**验收**：**流量/WiFi 都能开，断网也能开，图标是自定义的**

---

### 阶段 7 · 打磨（可选，按需挑）
- [ ] 深色模式（`prefers-color-scheme`）
- [ ] 快捷记账：打开就聚焦金额输入框
- [ ] 数据导出 JSON / CSV（备份用，换手机时救命）
- [ ] 编辑已有记录
- [ ] 常用备注一键填充

---

## ⚠️ 风险点与对策

| 风险 | 对策 |
|------|------|
| 手机连不上开发服务器 | 用 `--host` 启动；Windows 防火墙放行 Node；确认同一 WiFi |
| 数据丢失 | 阶段 7 的导出功能**强烈建议做**，localStorage 清缓存就没了 |
| iPhone 兼容性 | 当前只针对安卓。将来要支持 iPhone，注意 Safari 对 PWA 缓存更激进 |
| 金额精度 | 一律用 `Math.round(x * 100) / 100` 处理，避免浮点误差累积 |
| 分类写死不够用 | 先写死，真觉得不够再加"自定义分类"（要改存储结构，记得升版本号） |

---

## 📁 目标目录结构

```
记账app/
├── index.html
├── package.json
├── vite.config.js
├── public/
│   ├── manifest.json
│   ├── sw.js
│   └── icons/
│       ├── icon-192.png
│       └── icon-512.png
└── src/
    ├── main.js        # 入口：初始化、切换页面
    ├── store.js       # 数据层（增删查改 + 统计）
    ├── style.css
    └── views/
        ├── record.js  # 记一笔
        ├── list.js    # 列表
        └── stats.js   # 统计
```

---

## ✅ 推荐推进节奏

**阶段 0~2 一口气做完** → 这时已经有能用的东西了
**阶段 3~4** → 补齐查看和统计
**阶段 5~6** → 变成真 app，日常可用
**阶段 7** → 长期慢慢加

**建议**：每完成一个阶段就在手机上实测一次并提交一次 git。
个人项目最容易死在"一口气写完再测"，出了问题不知道是哪一步坏的。
