{
  "name": "ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "node test/store.test.mjs && node test/ui.test.mjs",
    "deploy": "bash scripts/deploy.sh"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
